import 'package:flutter/material.dart';

/// Application color palette
class AppColors {
  AppColors._();

  /// Primary brand color (OpenClaw Red)
  static const Color primary = Color(0xFFE63946);

  /// Secondary color
  static const Color secondary = Color(0xFF457B9D);

  /// Accent color
  static const Color accent = Color(0xFFF1FAEE);

  /// Success color
  static const Color success = Color(0xFF06D6A0);

  /// Warning color
  static const Color warning = Color(0xFFFFC857);

  /// Error color
  static const Color error = Color(0xFFEF476F);

  /// Info color
  static const Color info = Color(0xFF118AB2);

  /// Background colors
  static const Color backgroundLight = Color(0xFFF8F9FA);
  static const Color backgroundDark = Color(0xFF1A1A1A);

  /// Surface colors
  static const Color surfaceLight = Color(0xFFFFFFFF);
  static const Color surfaceDark = Color(0xFF2D2D2D);

  /// Text colors
  static const Color textPrimary = Color(0xFF1D3557);
  static const Color textSecondary = Color(0xFF6C757D);
  static const Color textLight = Color(0xFFE9ECEF);
}
