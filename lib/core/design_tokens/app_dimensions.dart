class TokenDimensions {
  TokenDimensions._();

  /// width.content — 335
  static const double contentWidth = 335.0;

  /// width.screen — 375
  static const double screenWidth = 375.0;
}

class TokenSize {
  TokenSize._();

  /// icon.small — 16
  static const double iconSmall = 16.0;

  /// progress.height — 8
  static const double progressHeight = 8.0;

  /// button.height — 50
  static const double buttonHeight = 50.0;

  /// stroke.thin — 0.5
  static const double strokeThin = 0.5;

  /// stroke.normal — 1
  static const double strokeNormal = 1.0;

  /// stroke.focus — 2 (WCAG 2.4.11 focus indicator min thickness). See IGN-303.
  static const double strokeFocus = 2.0;
}
