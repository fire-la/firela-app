---
phase: 06-settings-sync
type: execute
---

<objective>
Wire up existing settings page placeholders, add account settings section, and implement preferences management.

Purpose: Make the settings page fully functional with working navigation and preference persistence.
Output: Complete settings page with data export, about page, account settings, and preferences.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md

[Relevant source files:]
@lib/features/settings/presentation/pages/settings_page.dart
@lib/core/network/auth_manager.dart
@lib/core/services/auth_service.dart
@lib/l10n/app_en.arb
@lib/l10n/app_zh_CN.arb
@lib/l10n/app_zh_TW.arb

**Established patterns:**
- Settings items with navigation callbacks
- AlertDialog for confirmations
- Signal pattern for state management (themeSignal, localeSignal, regionSignal)
- ScaffoldMessenger for toast feedback
- Hive for persistent storage
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire up settings page placeholder items</name>
  <files>lib/features/settings/presentation/pages/settings_page.dart, lib/features/settings/presentation/pages/about_page.dart, lib/features/settings/presentation/pages/help_center_page.dart, lib/l10n/app_en.arb, lib/l10n/app_zh_CN.arb, lib/l10n/app_zh_TW.arb</files>
  <action>Implement the placeholder settings items:
1. **Data Export**: Show bottom sheet with export options (JSON, CSV). Export data via IgnApiService.exportData() method (add if needed). Show progress indicator during export. Save to device downloads or share via share_plus.
2. **About Page**: Create AboutPage with app version, build info, licenses (showLicensesPage), and links (privacy, terms, feedback).
3. **Help Center**: Create HelpCenterPage with FAQ accordion and contact support button (mailto link).
4. **Privacy Statement**: Open web view or show privacy policy dialog with content.
5. **Suggestions/Feedback**: Open mailto link or feedback form.
Add i18n keys: exportData, exportAsJson, exportAsCsv, exporting, exportSuccess, exportFailed, privacyPolicy, termsOfService, licenses, faq, contactSupport, sendFeedback.</action>
  <verify>flutter analyze passes, all settings items navigate or perform actions</verify>
  <done>All placeholder settings items are functional with navigation and actions</done>
</task>

<task type="auto">
  <name>Task 2: Add account settings section</name>
  <files>lib/features/settings/presentation/pages/settings_page.dart, lib/features/settings/presentation/pages/account_settings_page.dart, lib/l10n/app_en.arb, lib/l10n/app_zh_CN.arb, lib/l10n/app_zh_TW.arb</files>
  <action>Add account settings section to settings page:
1. Create AccountSettingsPage with:
   - User ID display (read-only, with copy button)
   - Display name edit (optional custom name)
   - Security token management (view saved token indicator, re-login option)
   - Account deletion (with confirmation, clears all data)
2. Add navigation to account settings from user profile section in settings page.
3. Store custom display name in Hive (StorageKeys.displayName).
4. Show account created date (from JWT or stored).
Add i18n keys: accountSettings, userId, displayName, customName, securityToken, savedToken, reLogin, deleteAccount, deleteAccountConfirm, deleteAccountWarning, accountCreated.</action>
  <verify>flutter analyze passes, account settings page displays and functions correctly</verify>
  <done>Account settings page with user info, name editing, and account management</done>
</task>

<task type="auto">
  <name>Task 3: Add preferences section</name>
  <files>lib/features/settings/presentation/pages/settings_page.dart, lib/shared/signals/preferences_signal.dart, lib/core/constants/storage_keys.dart, lib/l10n/app_en.arb, lib/l10n/app_zh_CN.arb, lib/l10n/app_zh_TW.arb</files>
  <action>Add preferences section to settings page with toggle options:
1. Create preferencesSignal using signals package for:
   - autoSync (bool, default true)
   - showNotifications (bool, default true)
   - defaultCurrency (String, from region)
   - decimalPrecision (int, default 2)
2. Add Preferences section below existing settings items:
   - Auto sync toggle
   - Notifications toggle (opens system settings reminder)
   - Decimal precision selector (dialog with 0-4 options)
3. Persist preferences to Hive using StorageKeys.preferencesBox.
4. Apply preferences to relevant features (e.g., currency formatting).
Add i18n keys: preferences, autoSync, notifications, decimalPrecision, systemSettings.</action>
  <verify>flutter analyze passes, preferences persist across app restarts</verify>
  <done>Preferences section with toggles and persistence working</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `flutter analyze` passes with no errors
- [ ] `flutter build apk --debug` succeeds
- [ ] All settings items navigate or perform actions
- [ ] Account settings page accessible and functional
- [ ] Preferences persist across restarts
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Settings page fully functional
- Account management working
- Preferences persist correctly
</success_criteria>

<output>
After completion, create `.planning/phases/06-settings-sync/06-01-SUMMARY.md`:

# Phase 06 Plan 01: Settings Page Implementation Summary

**[Substantive one-liner - what shipped, not "phase complete"]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `path/to/file.dart` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 06-02-PLAN.md
</output>
