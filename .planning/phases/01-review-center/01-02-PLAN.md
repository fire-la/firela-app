---
phase: 01-review-center
plan: 02
type: execute
---

<objective>
Add Review Center entry point with badge to the Assets page Tab bar.

Purpose: Provide visible entry point for users to access pending review transactions.
Output: Badge widget showing pending count, integrated into AssetsTabsPage.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/standards/error-handling.md
@.planning/phases/01-review-center/DISCOVERY.md

# Existing files to reference
@lib/features/assets/presentation/pages/assets_tabs_page.dart
@lib/core/router/app_router.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create review center badge widget</name>
  <files>lib/features/review_center/presentation/widgets/review_center_badge.dart</files>
  <action>
Create a reusable badge widget for the Review Center entry point:

1. **ReviewCenterBadge** widget:
   - Display an icon (e.g., Icons.fact_check or similar review-related icon)
   - Show number badge overlay when count > 0
   - Badge should show pending count from backend
   - Handle tap to navigate to Review Center page
   - Support loading and error states gracefully

2. Use signals for state management:
   - Create a signal for pending count
   - Widget should be reactive to count changes

3. Style:
   - Follow existing badge/notification patterns in the app
   - Red badge with white text for count
   - Proper sizing and positioning

Do NOT hardcode the count - fetch from repository.
  </action>
  <verify>
    - Widget exists and compiles
    - Badge displays correctly with count
    - Tap navigation works (test with placeholder route)
    - `flutter analyze lib/features/review_center/presentation/widgets/` passes
  </verify>
  <done>
    - ReviewCenterBadge widget with icon and number badge
    - Reactive to pending count changes
    - Proper styling matching app theme
    - Navigation handler implemented
  </done>
</task>

<task type="auto">
  <name>Task 2: Add badge to AssetsTabsPage</name>
  <files>lib/features/assets/presentation/pages/assets_tabs_page.dart</files>
  <action>
Integrate the ReviewCenterBadge into the AssetsTabsPage:

1. Locate the Tab bar area in AssetsTabsPage
2. Add ReviewCenterBadge to the right side of the Tab bar
3. Ensure proper layout:
   - Badge should not interfere with Tab functionality
   - Proper spacing and alignment
   - Responsive to different screen sizes

4. Initialize count fetching when page loads
5. Consider using a provider/signal to manage the count globally

Read the existing file first to understand the current structure before making changes.
  </action>
  <verify>
    - Badge appears on the right side of Tab bar
    - Does not break existing Tab functionality
    - Proper layout on different screen sizes
    - `flutter analyze` passes
  </verify>
  <done>
    - ReviewCenterBadge integrated into AssetsTabsPage
    - Positioned correctly (Tab bar right side)
    - Count fetched on page load
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Review Center routes to router</name>
  <files>lib/core/router/app_router.dart</files>
  <action>
Add routes for Review Center pages:

1. Add routes in app_router.dart:
   - `/review-center` - ReviewCenterPage (list)
   - `/review-center/:id` - ReviewDetailPage (detail)

2. Follow existing routing patterns in the file
3. Ensure routes are accessible from anywhere in the app

Note: The actual pages will be created in Plan 03 and 04. For now, create placeholder routes or verify the router structure is ready to accept them.

Read the existing router file first to understand the pattern.
  </action>
  <verify>
    - Routes defined in router
    - Routes follow existing patterns
    - `flutter analyze` passes
  </verify>
  <done>
    - `/review-center` route defined
    - `/review-center/:id` route defined
    - Router ready for page implementation
  </done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `flutter analyze` passes with no errors
- [ ] Badge widget displays correctly
- [ ] Badge integrated into AssetsTabsPage
- [ ] Routes defined in router
- [ ] No visual regressions in AssetsTabsPage
</verification>

<success_criteria>
- All 3 tasks completed
- Entry point visible and functional
- Routes ready for page implementation
- No breaking changes to existing functionality
</success_criteria>

<output>
After completion, create `.planning/phases/01-review-center/01-02-SUMMARY.md`:

# Phase 01 Plan 02: Review Center Entry Point Summary

**Added Review Center badge entry point to Assets page**

## Accomplishments
- Created ReviewCenterBadge widget with number badge
- Integrated badge into AssetsTabsPage Tab bar
- Added routes for Review Center pages

## Files Created/Modified
- `lib/features/review_center/presentation/widgets/review_center_badge.dart` - Badge widget
- `lib/features/assets/presentation/pages/assets_tabs_page.dart` - Added badge integration
- `lib/core/router/app_router.dart` - Added review center routes

## Decisions Made
- Badge position: Right side of Tab bar
- Badge fetches count on page load

## Issues Encountered
[None or specific issues]

## Next Step
Ready for 01-03-PLAN.md (List Page)
</output>
