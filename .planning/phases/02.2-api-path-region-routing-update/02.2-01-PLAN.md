---
phase: 02.2-api-path-region-routing-update
type: execute
---

<objective>
Update API infrastructure to support region-aware routing per latest API documentation.

Purpose: Align API paths with backend structure and enable multi-region support (cn, us, eu-core, de).
Output: Region-aware ApiClient, updated ApiConstants, region signal for state management.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md

[Region routing documentation:]
@region-routing-integration.md
@region-selection-integration.md

[Relevant source files:]
@lib/core/network/api_client.dart
@lib/core/constants/api_constants.dart
@lib/features/review_center/data/datasources/review_center_remote_datasource.dart

**Current Issues:**
- API paths use `/v1/bean/...` but should be `/api/v1/{region}/bean/...`
- Review Center uses `/v1/bean/pending-transactions` but should be `/api/v1/{region}/bean/review-center`
- No region management in the app
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ApiClient with region-aware URL building</name>
  <files>lib/core/network/api_client.dart</files>
  <action>Add region routing support to ApiClient:
1. Add `_currentRegion` field with default value 'cn'
2. Add `setRegion(String region)` method
3. Add `_isRegionScopedPath(String path)` helper - returns true for paths starting with `/bean/`, `/reporting/`, `/region/`
4. Modify `_buildUrl(String path)` to inject region prefix for region-scoped paths: `/api/v1/{region}{path}`
5. Global paths (auth, symbol, platforms) remain unchanged: `/api/v1{path}`
6. Update all URL construction in `_request()`, `uploadFile()`, `requestWithoutAuth()` to use `_buildUrl()`
7. Update baseUrl to include `/api` prefix: `https://api-s.firela.io/api`

Follow the pattern from region-routing-integration.md section 4.1-4.2.</action>
  <verify>`flutter analyze` passes without errors, ApiClient compiles correctly</verify>
  <done>ApiClient builds region-aware URLs: `/api/v1/cn/bean/transactions` for region-scoped paths</done>
</task>

<task type="auto">
  <name>Task 2: Create region signal and update ApiConstants</name>
  <files>lib/shared/signals/region_signal.dart, lib/core/constants/api_constants.dart</files>
  <action>Create region signal and update constants:

1. Create `lib/shared/signals/region_signal.dart`:
   - Define `RegionCode` typedef or enum: 'cn', 'us', 'eu-core', 'de'
   - Create `regionSignal` with default 'cn'
   - Create `RegionInfo` class with code, name, flag, currency, locale
   - Create `REGION_INFO` map with all supported regions
   - Add `setRegion(RegionCode region)` function that updates signal AND ApiClient.instance.setRegion()

2. Update `lib/core/constants/api_constants.dart`:
   - Change all paths from `/v1/bean/...` to `/bean/...` (ApiClient will add `/api/v1/{region}` prefix)
   - Global paths: `/auth/...`, `/symbol/...`, `/platforms/...` (no region prefix)
   - Fix Review Center: change `/v1/bean/pending-transactions` references to `/bean/review-center`
   - Add comment explaining region routing pattern

Follow project conventions: snake_case files, camelCase functions, singleton pattern for services.</action>
  <verify>`flutter analyze` passes, region signal works, ApiConstants updated correctly</verify>
  <done>Region signal created and connected to ApiClient, all endpoints updated to new path format</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `flutter analyze` passes with no errors
- [ ] ApiClient correctly builds region-scoped URLs
- [ ] Global paths (auth, symbol, platforms) work without region prefix
- [ ] Region signal properly connected to ApiClient
</verification>

<success_criteria>

- All tasks completed
- ApiClient supports region-aware URL building
- Region signal created with 'cn' default
- API constants updated to new path format
- No breaking changes to existing functionality
</success_criteria>

<output>
After completion, create `.planning/phases/02.2-api-path-region-routing-update/02.2-01-SUMMARY.md`:

# Phase 2.2 Plan 01: Region-Aware API Infrastructure Summary

**Region routing support in ApiClient with signal-based region management**

## Accomplishments

- ApiClient updated with region-aware URL building
- Region signal created with default 'cn' region
- API constants updated to new path format

## Files Created/Modified

- `lib/core/network/api_client.dart` - Added region routing
- `lib/shared/signals/region_signal.dart` - Created region signal
- `lib/core/constants/api_constants.dart` - Updated endpoint paths

## Decisions Made

Use signal pattern for region state management
Inject region prefix automatically in ApiClient
Default region is 'cn'

## Issues Encountered

None

## Next Step

Ready for 02.2-02-PLAN.md - Update feature datasources with correct endpoints
</output>
