---
phase: 03-bill-import-enhancement
type: execute
---

<objective>
Add OCR/receipt scanning capability to bill import, allowing users to capture or select receipt images for automatic expense recognition.

Purpose: Enable mobile-first expense capture by allowing users to photograph receipts instead of importing CSV files.
Output: Bill import page with camera/gallery options and OCR processing flow.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/INTEGRATIONS.md

[Relevant source files:]
@lib/features/expense/presentation/pages/bill_import_page.dart
@lib/core/services/ign_api_service.dart
@lib/core/services/analytics_service.dart
@lib/core/services/analytics_events.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add OCR service layer</name>
  <files>lib/core/services/ocr_service.dart, lib/core/services/ign_api_service.dart</files>
  <action>Create `OcrService` class with `processReceiptImage(String imagePath)` method that uploads image to backend OCR endpoint (POST /v1/bean/ocr/receipt). Return parsed receipt data with merchant, amount, date, line items, and confidence score. Add OCR endpoint to IgnApiService if not present. Use multipart/form-data for image upload. Handle errors gracefully with user-friendly messages.</action>
  <verify>flutter analyze passes with no errors</verify>
  <done>OcrService exists with processReceiptImage method, image upload works with backend</done>
</task>

<task type="auto">
  <name>Task 2: Add OCR analytics events</name>
  <files>lib/core/services/analytics_events.dart, lib/core/services/analytics_service.dart</files>
  <action>Add OCR-related event constants to AnalyticsEvents: ocr_image_selected, ocr_processing_started, ocr_processing_success, ocr_processing_failed. Add trackOcr() helper to AnalyticsService with properties: source (camera/gallery), confidence_score, merchant_detected, amount_detected. Follow snake_case naming pattern established in Phase 2.</action>
  <verify>flutter analyze passes with no errors</verify>
  <done>OCR event constants defined, trackOcr helper added to AnalyticsService</done>
</task>

<task type="auto">
  <name>Task 3: Add image capture options to bill import page</name>
  <files>lib/features/expense/presentation/pages/bill_import_page.dart, lib/l10n/app_zh_CN.arb, lib/l10n/app_zh_TW.arb</files>
  <action>Modify bill_import_page.dart to show two options: "Import File" (existing) and "Scan Receipt" (new). Add bottom sheet or dialog with camera/gallery options when "Scan Receipt" tapped. Use image_picker package (already in dependencies) to capture image. Show image preview with "Process" button. Call OcrService.processReceiptImage() and navigate to review on success. Add i18n strings: scanReceipt, takePhoto, chooseFromGallery, processing, scanReceiptTitle. Track analytics for image source selection.</action>
  <verify>flutter analyze passes, app builds without errors</verify>
  <done>Bill import page has file and scan options, camera/gallery works, OCR flow functional</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `flutter analyze` passes with no errors
- [ ] `flutter build apk --debug` succeeds
- [ ] OCR service compiles and integrates with existing architecture
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- OCR image capture flow works end-to-end
- Analytics tracking for OCR events in place
</success_criteria>

<output>
After completion, create `.planning/phases/03-bill-import-enhancement/03-01-SUMMARY.md`:

# Phase 03 Plan 01: OCR/Receipt Scanning Integration Summary

**[Substantive one-liner - what shipped, not "phase complete"]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `path/to/file.dart` - Description
- `path/to/another.dart` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 03-02-PLAN.md: NLP Expense Categorization Enhancement
</output>
