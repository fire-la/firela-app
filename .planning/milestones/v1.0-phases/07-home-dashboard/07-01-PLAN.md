---
phase: 07-home-dashboard
plan: 01
type: execute
---

<objective>
Create home dashboard layout with summary widgets displaying net worth, pending reviews count, and FIRE progress overview.

Purpose: Provide users with an at-a-glance view of their financial status when opening the app.
Output: Functional home dashboard page with summary cards and widgets.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Codebase standards
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md

# Prior phase summaries (dependencies)
@.planning/phases/06-settings-sync/06-03-SUMMARY.md
@.planning/phases/05-fire-journey-tracker/05-03-SUMMARY.md

# Key source files for reference
@lib/features/main/presentation/pages/main_page.dart
@lib/features/review_center/presentation/signals/review_center_signal.dart
@lib/features/fire_journey/presentation/providers/use_fire_progress.dart
@lib/features/fire_journey/data/services/fire_calculation_service.dart
@lib/features/assets/data/services/dashboard_aggregation_service.dart

**Tech stack available:**
- Flutter 3.24.0+ / Dart 3.5+
- signals_flutter for state management
- flutter_hooks for local state
- go_router for navigation
- fl_chart for charts
- connectivity_plus for network detection

**Established patterns:**
- Signal pattern for global state (pendingCountSignal, themeSignal, connectivitySignal)
- Hook pattern for data fetching (useFireProgress)
- Singleton service pattern (DashboardAggregationService, FireCalculationService)
- Feature-based structure with presentation/domain/data layers
- i18n with ARB files (app_en.arb, app_zh_CN.arb, app_zh_TW.arb)

**Constraining decisions:**
- [Phase 01]: Delete requires confirmation dialog, Toast feedback for all actions
- [Phase 02]: Use snake_case for route names and event names
- [Phase 04]: Use singleton pattern for aggregation services
- [Phase 06]: OfflineIndicator shows banner when disconnected
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HomePage with dashboard layout</name>
  <files>
    lib/features/home/presentation/pages/home_page.dart,
    lib/core/router/app_router.dart,
    lib/core/router/route_names.dart
  </files>
  <action>
    Create HomePage as a HookWidget with:
    1. Scaffold with AppBar showing greeting and last sync time
    2. Pull-to-refresh using RefreshIndicator
    3. SingleChildScrollView with vertical layout for summary cards
    4. Use Watch widget for reactive updates from signals

    Update app_router.dart to add home route:
    - Add RouteNames.home constant = '/home'
    - Add GoRoute for home page

    Follow existing page patterns from FireJourneyPage and AssetsTabsPage.
    Use SafeArea and proper padding (16.0 horizontal).
  </action>
  <verify>flutter analyze passes, home page renders without errors</verify>
  <done>HomePage created and accessible via route, layout follows codebase patterns</done>
</task>

<task type="auto">
  <name>Task 2: Create net worth summary card widget</name>
  <files>
    lib/features/home/presentation/widgets/net_worth_card.dart,
    lib/features/home/presentation/providers/use_home_data.dart,
    lib/l10n/app_en.arb,
    lib/l10n/app_zh_CN.arb,
    lib/l10n/app_zh_TW.arb
  </files>
  <action>
    Create NetWorthCard widget displaying:
    1. Total net worth amount (formatted with currency)
    2. Trend indicator (up/down arrow with percentage)
    3. Period label (e.g., "This month")
    4. Tap to navigate to Assets page

    Create useHomeData hook to fetch:
    - Net worth from IgnApiService.instance.getNetWorth()
    - Handle loading and error states
    - Support pull-to-refresh

    Add i18n keys: homeNetWorth, homeNetWorthTrend, homeThisMonth, homeLoading

    Use existing patterns:
    - Gradient background (dark theme, consistent with app design)
    - LoadingIndicator for loading state
    - Currency formatting from existing formatters
  </action>
  <verify>flutter analyze passes, card displays net worth with trend when data available</verify>
  <done>NetWorthCard displays net worth with trend, navigates to assets on tap, supports i18n</done>
</task>

<task type="auto">
  <name>Task 3: Add pending reviews and FIRE progress summary widgets</name>
  <files>
    lib/features/home/presentation/widgets/pending_reviews_card.dart,
    lib/features/home/presentation/widgets/fire_progress_card.dart,
    lib/features/home/presentation/pages/home_page.dart,
    lib/l10n/app_en.arb,
    lib/l10n/app_zh_CN.arb,
    lib/l10n/app_zh_TW.arb
  </files>
  <action>
    Create PendingReviewsCard widget:
    1. Display pending transaction count from pendingCountSignal
    2. Show breakdown by confidence level (high/medium/low) if available
    3. Tap to navigate to review center
    4. Use badge/notification style for count

    Create FireProgressCard widget:
    1. Display progress percentage from useFireProgress hook
    2. Show years to FIRE estimate
    3. Show savings rate
    4. Tap to navigate to FIRE Journey page
    5. Handle "no goal set" state with CTA to set goal

    Integrate both cards into HomePage below NetWorthCard.
    Use horizontal scrolling for cards if needed.

    Add i18n keys: homePendingReviews, homeFireProgress, homeYearsToFire, homeSavingsRate, homeSetGoal
  </action>
  <verify>flutter analyze passes, both cards display correct data and navigate on tap</verify>
  <done>PendingReviewsCard and FireProgressCard created, integrated into HomePage, support i18n</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Home dashboard with net worth, pending reviews, and FIRE progress summary cards</what-built>
  <how-to-verify>
    1. Run: flutter run
    2. Navigate to home page (update main_page.dart temporarily or access via route)
    3. Verify net worth card displays with correct amount and trend
    4. Verify pending reviews count shows correctly (if pending transactions exist)
    5. Verify FIRE progress displays (if goal set) or "set goal" CTA (if no goal)
    6. Test pull-to-refresh functionality
    7. Verify tap navigation to respective pages works
    8. Check offline indicator appears when disconnected
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `flutter analyze` passes with no errors
- [ ] HomePage renders without crashes
- [ ] NetWorthCard displays net worth and trend
- [ ] PendingReviewsCard displays pending count from signal
- [ ] FireProgressCard displays progress or "set goal" CTA
- [ ] All cards navigate to correct pages on tap
- [ ] i18n keys added for all 3 languages
- [ ] Pull-to-refresh works
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript/linting errors
- Home dashboard provides clear financial overview
- Cards follow existing UI patterns and theming
</success_criteria>

<output>
After completion, create `.planning/phases/07-home-dashboard/07-01-SUMMARY.md`:

# Phase 07 Plan 01: Dashboard Layout and Summary Widgets Summary

**[Substantive one-liner - what shipped, not "phase complete"]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `path/to/file.dart` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 07-02-PLAN.md: Quick Actions and Shortcuts
</output>
