---
phase: 05-fire-journey-tracker
type: execute
---

<objective>
Create FIRE calculation engine with goal setting, progress tracking, and time-to-FIRE projections.

Purpose: Enable users to understand when they can achieve financial independence based on their current financial trajectory.
Output: FIRE calculation service, goal models, and progress calculations.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md

[Relevant source files:]
@lib/features/fire_journey/presentation/pages/fire_journey_page.dart
@lib/features/assets/data/services/dashboard_aggregation_service.dart
@lib/features/assets/domain/models/net_worth_history_point.dart
@lib/core/services/ign_api_service.dart
@lib/l10n/app_en.arb
@lib/l10n/app_zh_CN.arb
@lib/l10n/app_zh_TW.arb

**Established patterns:**
- Singleton service pattern (DashboardAggregationService)
- Immutable domain models with fromJson factories
- Signals for reactive state management
- flutter_hooks for widget lifecycle

**Constraining decisions:**
- [Phase 04-01]: DashboardAggregationService available for net worth history
- [Phase 04-01]: NetWorthHistoryPoint model available
- Use 4% rule for safe withdrawal rate (25x annual expenses = FIRE number)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FIRE goal and progress domain models</name>
  <files>lib/features/fire_journey/domain/models/fire_goal.dart, lib/features/fire_journey/domain/models/fire_progress.dart, lib/features/fire_journey/domain/models/fire_milestone.dart</files>
  <action>Create domain models:
1. FireGoal: targetAmount (double), targetDate (DateTime?), monthlyExpenses (double), withdrawalRate (double, default 0.04), created/updated timestamps
2. FireProgress: currentNetWorth (double), fireNumber (double), progressPercentage (double), yearsToFire (double?), monthlySavings (double), savingsRate (double)
3. FireMilestone: id (String), name (String), targetAmount (double), achieved (bool), achievedDate (DateTime?)
All models should have fromJson/toJson, be immutable with const constructors. Add i18n keys: fireGoal, fireNumber, savingsRate, yearsToFire.</action>
  <verify>flutter analyze passes with no errors</verify>
  <done>FIRE domain models ready for calculation engine</done>
</task>

<task type="auto">
  <name>Task 2: Create FIRE calculation service</name>
  <files>lib/features/fire_journey/data/services/fire_calculation_service.dart</files>
  <action>Create FireCalculationService as singleton with methods:
1. `calculateFireNumber(double monthlyExpenses, {double withdrawalRate = 0.04})` - returns 25x annual expenses
2. `calculateProgress(double currentNetWorth, double fireNumber)` - returns percentage
3. `calculateYearsToFire(double currentNetWorth, double fireNumber, double monthlySavings, {double annualReturn = 0.07})` - uses compound interest formula
4. `calculateSavingsRate(double monthlyIncome, double monthlyExpenses)` - returns percentage saved
5. `calculateMonthlySavings(double currentNetWorth, double fireNumber, int targetYears, {double annualReturn = 0.07})` - reverse calculation
Use standard FIRE formulas. Add logging for calculations.</action>
  <verify>flutter analyze passes with no errors</verify>
  <done>FIRE calculation service provides all necessary financial projections</done>
</task>

<task type="auto">
  <name>Task 3: Add FIRE goal storage and API endpoint</name>
  <files>lib/core/services/ign_api_service.dart, lib/core/constants/api_constants.dart, lib/features/fire_journey/data/services/fire_goal_storage.dart</files>
  <action>Add FIRE goal persistence:
1. Add `getFireGoal()` and `saveFireGoal(FireGoal goal)` to IgnApiService
2. Add endpoint constants: `fireGoalEndpoint = '/bean/fire-goal'`
3. Create FireGoalStorage service for local caching with Hive
4. Handle offline scenarios - cache goal locally, sync when online
5. Add loading and error states
The goal includes target FIRE number and monthly expense target.</action>
  <verify>flutter analyze passes with no errors</verify>
  <done>FIRE goal can be saved and retrieved from backend</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `flutter analyze` passes with no errors
- [ ] `flutter build apk --debug` succeeds
- [ ] FIRE calculation formulas are mathematically correct
- [ ] Domain models follow established patterns
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- FIRE calculation service provides accurate projections
- Goal persistence works with offline support
- Ready for progress tracking implementation
</success_criteria>

<output>
After completion, create `.planning/phases/05-fire-journey-tracker/05-01-SUMMARY.md`:

# Phase 05 Plan 01: FIRE Calculation Engine Summary

**[Substantive one-liner - what shipped, not "phase complete"]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `path/to/file.dart` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 05-02-PLAN.md
</output>
