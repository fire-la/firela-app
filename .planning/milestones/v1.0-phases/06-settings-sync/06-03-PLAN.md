---
phase: 06-settings-sync
type: execute
---

<objective>
Add offline mode support with data caching, conflict resolution, and offline indicator UI.

Purpose: Enable users to use the app offline with automatic sync when connectivity is restored.
Output: Offline caching, conflict resolution logic, and connectivity status UI.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md

[Relevant source files:]
@lib/core/services/sync_service.dart
@lib/core/services/ign_api_service.dart
@lib/core/network/api_client.dart
@lib/features/review_center/data/datasources/review_center_remote_datasource.dart
@lib/l10n/app_en.arb
@lib/l10n/app_zh_CN.arb
@lib/l10n/app_zh_TW.arb

**Established patterns:**
- Hive for local storage and caching
- connectivity_plus package for network detection
- Signal pattern for reactive state
- Pending transaction model already has local caching
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add connectivity monitoring and offline indicator</name>
  <files>lib/shared/signals/connectivity_signal.dart, lib/shared/widgets/offline_indicator.dart, lib/features/main/presentation/pages/main_page.dart, lib/l10n/app_en.arb, lib/l10n/app_zh_CN.arb, lib/l10n/app_zh_TW.arb</files>
  <action>Add connectivity monitoring:
1. Add connectivity_plus package to pubspec.yaml if not present.
2. Create connectivitySignal using signals package:
   - Listen to connectivity_plus stream
   - Expose isOnline (bool) and connectionType (wifi/mobile/none)
3. Create OfflineIndicator widget:
   - Banner at top of screen when offline
   - Shows "You are offline" message with sync pending indicator
   - Yellow/warning color scheme
   - Auto-hides when online
4. Add OfflineIndicator to MainPage (below AppBar, above body).
5. Add offline-aware toast when actions fail due to no connection.
Add i18n keys: offline, youAreOffline, syncPending, connectionRestored, actionFailedOffline.</action>
  <verify>flutter analyze passes, offline indicator shows when disconnected</verify>
  <done>Connectivity monitoring with offline indicator working</done>
</task>

<task type="auto">
  <name>Task 2: Add offline data caching for key features</name>
  <files>lib/core/services/offline_cache_service.dart, lib/core/services/ign_api_service.dart, lib/features/review_center/data/datasources/review_center_remote_datasource.dart, lib/features/assets/data/services/dashboard_aggregation_service.dart, lib/core/constants/storage_keys.dart</files>
  <action>Add offline caching layer:
1. Create OfflineCacheService singleton with:
   - `cacheData(String key, dynamic data, {Duration? expiry})` - Store with optional TTL
   - `getCachedData<T>(String key)` - Retrieve cached data
   - `invalidateCache(String key)` - Clear specific cache
   - `clearAllCache()` - Clear all cached data
2. Cache key API responses:
   - Pending transactions (cache on fetch, serve from cache when offline)
   - Net worth history (cache daily snapshots)
   - FIRE goal and progress
3. Modify IgnApiService to check cache on network failure:
   - Try network request
   - On failure, return cached data if available
   - Mark response as stale if from cache
4. Add pending operations queue for write operations when offline:
   - Store pending confirmTransaction, updateTransaction in Hive
   - Process queue when connectivity restored
Add StorageKeys for cache boxes.</action>
  <verify>flutter analyze passes, cached data serves when offline</verify>
  <done>Offline caching serves stale data when network unavailable</done>
</task>

<task type="auto">
  <name>Task 3: Add conflict resolution for sync</name>
  <files>lib/core/services/conflict_resolution_service.dart, lib/core/services/sync_service.dart, lib/features/settings/presentation/dialogs/conflict_dialog.dart, lib/l10n/app_en.arb, lib/l10n/app_zh_CN.arb, lib/l10n/app_zh_TW.arb</files>
  <action>Add conflict resolution:
1. Create ConflictResolutionService with strategies:
   - `serverWins` - Server data overwrites local
   - `localWins` - Local data overwrites server
   - `newerWins` - Compare timestamps, keep newer
   - `manual` - Show dialog for user to choose
2. Add conflict detection in SyncService:
   - Compare local updatedAt with server updatedAt
   - Detect if both modified since last sync
3. Create ConflictDialog for manual resolution:
   - Show local vs server data comparison
   - Options: Keep Local, Keep Server, Keep Both (for non-conflicting fields)
4. Default strategy: newerWins for most data, manual for critical conflicts.
5. Log all conflicts for debugging.
Add i18n keys: syncConflict, localVersion, serverVersion, keepLocal, keepServer, keepBoth, conflictResolved.</action>
  <verify>flutter analyze passes, conflict resolution handles sync conflicts</verify>
  <done>Conflict resolution with automatic and manual strategies working</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `flutter analyze` passes with no errors
- [ ] `flutter build apk --debug` succeeds
- [ ] Offline indicator shows when disconnected
- [ ] Cached data serves when offline
- [ ] Pending operations sync when restored
- [ ] Conflict resolution handles sync conflicts
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Offline mode functional with cached data
- Sync conflicts resolved appropriately
- Phase 6 complete, ready for Phase 7
</success_criteria>

<output>
After completion, create `.planning/phases/06-settings-sync/06-03-SUMMARY.md`:

# Phase 06 Plan 03: Offline Mode and Conflict Resolution Summary

**[Substantive one-liner - what shipped, not "phase complete"]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `path/to/file.dart` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Phase 6 complete, ready for Phase 7: Home Dashboard
</output>
