---
phase: 02.2-api-path-region-routing-update
type: execute
---

<objective>
Update all feature datasources to use correct API endpoints per latest documentation.

Purpose: Fix incorrect API paths in Review Center and other datasources to match backend structure.
Output: All datasources using correct `/api/v1/{region}/bean/*` endpoints.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md

[Region routing documentation:]
@region-routing-integration.md

[Relevant source files:]
@lib/features/review_center/data/datasources/review_center_remote_datasource.dart
@lib/core/services/ign_api_service.dart
@lib/core/constants/api_constants.dart

**API Endpoint Changes (per region-routing-integration.md):**
- Review Center: `/bean/review-center` (GET list, POST /:id/accept, POST /:id/reject)
- NOT `/bean/pending-transactions` as currently used
- All Bean module endpoints need region prefix
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ReviewCenterRemoteDatasource with correct endpoints</name>
  <files>lib/features/review_center/data/datasources/review_center_remote_datasource.dart</files>
  <action>Update ReviewCenterRemoteDatasource to use correct API endpoints:
1. Change `_basePath` from `/v1/bean/pending-transactions` to `/bean/review-center`
2. Update `getPendingTransactions()` to use GET `/bean/review-center`
3. Update `getPendingTransactionDetail()` to use GET `/bean/review-center/:id`
4. Change `confirmTransaction()` to use POST `/bean/review-center/:id/accept` (was `/confirm`)
5. Add `rejectTransaction()` method using POST `/bean/review-center/:id/reject`
6. Keep `updateTransaction()` as PUT `/bean/review-center/:id`
7. Keep `deleteTransaction()` as DELETE `/bean/review-center/:id`
8. Update `getPendingCount()` to use GET `/bean/review-center/count` if available, or parse from list response
9. Update all logger messages to reflect correct endpoint names
10. Remove TODO comment about confirming endpoint paths

Note: The ApiClient will automatically inject `/api/v1/{region}` prefix.</action>
  <verify>`flutter analyze` passes, ReviewCenter datasource compiles without errors</verify>
  <done>ReviewCenter datasource uses correct `/bean/review-center` endpoints with accept/reject actions</done>
</task>

<task type="auto">
  <name>Task 2: Update IgnApiService and verify all API usage</name>
  <files>lib/core/services/ign_api_service.dart</files>
  <action>Update IgnApiService and verify API path consistency:
1. Read `ign_api_service.dart` and identify any hardcoded paths
2. Update any paths that use old `/v1/bean/...` format to use ApiConstants
3. Ensure all Bean module API calls go through ApiClient (which adds region prefix)
4. Search for any other files using hardcoded `/v1/` paths and update them
5. Run grep to verify no remaining old-style paths: `grep -r "/v1/bean" lib/`
6. Update any test mocks if they reference old paths

Verify the following files use correct patterns:
- lib/core/services/ign_api_service.dart
- lib/features/review_center/data/datasources/
- lib/features/expense/data/datasources/ (if exists)</action>
  <verify>`grep -r "/v1/bean" lib/` returns no matches, all API calls use ApiConstants</verify>
  <done>All datasources use correct API paths via ApiConstants, no hardcoded old-style paths remain</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `flutter analyze` passes with no errors
- [ ] `flutter test` passes
- [ ] No old-style `/v1/bean` paths in codebase
- [ ] Review Center uses `/bean/review-center` endpoint
- [ ] All Bean module calls go through ApiClient
</verification>

<success_criteria>

- All tasks completed
- Review Center datasource uses correct endpoints
- No hardcoded old-style API paths remain
- All Bean module API calls properly routed through ApiClient
- Tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02.2-api-path-region-routing-update/02.2-02-SUMMARY.md`:

# Phase 2.2 Plan 02: Feature Datasource Updates Summary

**Updated all feature datasources with correct API endpoints**

## Accomplishments

- ReviewCenter datasource updated to use `/bean/review-center`
- Added accept/reject transaction methods
- Removed all hardcoded old-style API paths
- Verified API path consistency across codebase

## Files Created/Modified

- `lib/features/review_center/data/datasources/review_center_remote_datasource.dart` - Updated endpoints
- `lib/core/services/ign_api_service.dart` - Updated paths to use ApiConstants

## Decisions Made

Use `/bean/review-center` endpoint for Review Center (not pending-transactions)
accept/reject pattern for reviewing transactions
All paths centralized in ApiConstants

## Issues Encountered

None

## Next Step

Phase 2.2 complete - ready to continue with Phase 3 Bill Import Enhancement
</output>
