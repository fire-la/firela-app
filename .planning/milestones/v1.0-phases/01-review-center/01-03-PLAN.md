---
phase: 01-review-center
plan: 03
type: execute
---

<objective>
Create the Review Center list page with confidence level tabs, transaction cards, and infinite scroll.

Purpose: Allow users to browse and manage pending review transactions with filtering by confidence level.
Output: Fully functional list page with 4 tabs, transaction cards, and pagination.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/standards/error-handling.md
@.planning/phases/01-review-center/DISCOVERY.md

# Existing patterns to follow
@lib/features/assets/presentation/pages/assets_tabs_page.dart
@lib/features/expense/presentation/pages/expense_page.dart
@lib/core/services/ign_api_service.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create confidence indicator widget</name>
  <files>lib/features/review_center/presentation/widgets/confidence_indicator.dart</files>
  <action>
Create a widget to visually indicate confidence level:

1. **ConfidenceIndicator** widget:
   - Display visual indicator (colored dot, icon, or badge)
   - Color coding:
     - HIGH (≥85%): Green
     - MEDIUM (60-84%): Orange/Yellow
     - LOW (<60%): Red
   - Optional: Show percentage or text label
   - Compact size suitable for card display

2. Accept ConfidenceLevel enum as input
3. Follow existing widget patterns in the app for styling
  </action>
  <verify>
    - Widget compiles and displays correctly
    - Colors match confidence levels
    - Compact and suitable for card display
    - `flutter analyze` passes
  </verify>
  <done>
    - ConfidenceIndicator widget with color-coded display
    - Works with ConfidenceLevel enum
    - Proper styling
  </done>
</task>

<task type="auto">
  <name>Task 2: Create pending transaction card widget</name>
  <files>lib/features/review_center/presentation/widgets/pending_transaction_card.dart</files>
  <action>
Create the transaction card widget following product design:

1. **PendingTransactionCard** widget:
   - Display fields:
     - Account name (账户名称)
     - Merchant name (商户名)
     - Transaction amount (交易金额)
     - Transaction time (交易时间)
     - Confidence indicator
   - Action buttons at bottom:
     - 删除 (Delete) button - secondary/destructive style
     - 保留这笔 (Keep) button - primary style
   - Entire card is tappable → navigate to detail page

2. Callbacks:
   - `onDelete()` - Called when delete button tapped
   - `onConfirm()` - Called when keep button tapped
   - `onTap()` - Called when card tapped (navigate to detail)

3. Style:
   - Card with rounded corners
   - Proper padding and spacing
   - Amount prominently displayed
   - Follow existing card styles in the app
  </action>
  <verify>
    - Widget displays all required fields
    - Buttons styled correctly
    - Tap handling works
    - `flutter analyze` passes
  </verify>
  <done>
    - PendingTransactionCard with all display fields
    - Delete and Keep buttons
    - Card tap navigation
    - Proper styling
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Review Center list page with tabs</name>
  <files>lib/features/review_center/presentation/pages/review_center_page.dart, lib/features/review_center/presentation/providers/review_center_provider.dart</files>
  <action>
Create the main Review Center page with all features:

1. **ReviewCenterProvider** (state management):
   - Manage list of pending transactions
   - Track current tab/filter
   - Handle pagination (page, hasMore)
   - Track loading and error states
   - Track tab counts
   - Methods:
     - `loadTransactions(refresh: bool)` - Load initial/refresh
     - `loadMore()` - Load next page
     - `confirmTransaction(id)` - Confirm and remove from list
     - `deleteTransaction(id)` - Delete and remove from list
     - `changeTab(ConfidenceLevel)` - Switch tab

2. **ReviewCenterPage**:
   - AppBar with title "审核中心"
   - 4 tabs at top:
     - 全部 (All)
     - 高置信度 (HIGH)
     - 需确认 (MEDIUM)
     - 建议核对 (LOW)
   - Tab labels show count: e.g., "需确认(12)"
   - List view below tabs:
     - Infinite scroll (ScrollController with loadMore)
     - Pull-to-refresh
     - PendingTransactionCard for each item
   - Empty state when no items
   - Loading indicator while fetching

3. Use flutter_hooks and signals_flutter for state

4. Implement:
   - Pull-to-refresh with RefreshIndicator
   - Infinite scroll detection
   - Tab switching with data reload
   - Confirmation dialog for delete
   - Toast feedback for confirm/delete actions
  </action>
  <verify>
    - Page displays with 4 tabs
    - Tab counts displayed correctly
    - Infinite scroll works
    - Pull-to-refresh works
    - Empty state shows when no data
    - Delete confirmation dialog appears
    - Toast shows after confirm/delete
    - `flutter analyze` passes
  </verify>
  <done>
    - ReviewCenterProvider with full state management
    - ReviewCenterPage with tabs, list, pagination
    - Pull-to-refresh and infinite scroll
    - Delete confirmation and Toast feedback
  </done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `flutter analyze lib/features/review_center/` passes
- [ ] All 4 tabs display correctly
- [ ] Infinite scroll loads more data
- [ ] Pull-to-refresh works
- [ ] Delete shows confirmation dialog
- [ ] Toast appears after actions
- [ ] Empty state displays when no data
</verification>

<success_criteria>
- All 3 tasks completed
- List page fully functional
- All UI interactions working
- Proper error handling and feedback
</success_criteria>

<output>
After completion, create `.planning/phases/01-review-center/01-03-SUMMARY.md`:

# Phase 01 Plan 03: Review Center List Page Summary

**Created Review Center list page with tabs and infinite scroll**

## Accomplishments
- Created ConfidenceIndicator widget
- Created PendingTransactionCard widget
- Created ReviewCenterProvider for state management
- Created ReviewCenterPage with 4 tabs
- Implemented infinite scroll and pull-to-refresh
- Added delete confirmation and toast feedback

## Files Created/Modified
- `lib/features/review_center/presentation/widgets/confidence_indicator.dart` - Confidence display
- `lib/features/review_center/presentation/widgets/pending_transaction_card.dart` - Transaction card
- `lib/features/review_center/presentation/providers/review_center_provider.dart` - State management
- `lib/features/review_center/presentation/pages/review_center_page.dart` - Main list page

## Decisions Made
- Used signals for state management
- Tab labels show counts
- Delete requires confirmation dialog

## Issues Encountered
[None or specific issues]

## Next Step
Ready for 01-04-PLAN.md (Detail Page)
</output>
