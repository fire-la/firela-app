---
phase: 04-assets-liabilities-dashboard
type: execute
---

<objective>
Add historical data fetching for assets dashboard, implement time-series data models, and create aggregation services for chart visualization.

Purpose: Enable the dashboard to display real historical trends instead of mock data.
Output: Historical net worth API, time-series models, and data aggregation utilities.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md

[Relevant source files:]
@lib/core/services/ign_api_service.dart
@lib/core/constants/api_constants.dart
@lib/features/assets/presentation/pages/assets_tabs_page.dart
@lib/features/assets/presentation/pages/assets_liabilities_page.dart

**Established patterns:**
- IgnApiService for API calls with region routing
- Clean architecture with data/domain/presentation layers
- Signals for state management
- flutter_hooks for widget lifecycle

**Constraining decisions:**
- [Phase 02.2-01]: Region prefix injected automatically in ApiClient._buildUrl()
- [Phase 02.2-01]: baseUrl includes /api prefix (https://api-s.firela.io/api)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add historical net worth API endpoint</name>
  <files>lib/core/services/ign_api_service.dart, lib/core/constants/api_constants.dart</files>
  <action>Add `getNetWorthHistory({required int months})` method to IgnApiService. This fetches historical net worth data for the specified number of months. Add corresponding endpoint constant `netWorthHistoryEndpoint = '/dashboard/net-worth/history'`. The API returns an array of `{date, netWorth, totalAssets, totalLiabilities}` objects. Use existing ApiClient.get() pattern with query params.</action>
  <verify>flutter analyze passes with no errors</verify>
  <done>API method available to fetch historical net worth data for charts</done>
</task>

<task type="auto">
  <name>Task 2: Create time-series data models</name>
  <files>lib/features/assets/domain/models/net_worth_history_point.dart, lib/features/assets/domain/models/asset_distribution.dart, lib/features/assets/domain/models/liability_breakdown.dart</files>
  <action>Create domain models for dashboard data:
1. NetWorthHistoryPoint: date (DateTime), netWorth (double), totalAssets (double), totalLiabilities (double)
2. AssetDistribution: category (String), amount (double), percentage (double), color (Color optional)
3. LiabilityBreakdown: type (String), amount (double), count (int), percentage (double)
All models should have fromJson() factories and be immutable with const constructors. Follow existing pattern from pending_transaction.dart.</action>
  <verify>flutter analyze passes with no errors</verify>
  <done>Domain models ready for dashboard data representation</done>
</task>

<task type="auto">
  <name>Task 3: Create dashboard data aggregation service</name>
  <files>lib/features/assets/data/services/dashboard_aggregation_service.dart</files>
  <action>Create DashboardAggregationService as singleton with methods:
1. `aggregateNetWorthHistory(List<Map<String, dynamic>> rawData)` - converts API response to List<NetWorthHistoryPoint>
2. `calculateAssetDistribution(List<AccountData> accounts)` - groups accounts by platform, calculates percentages
3. `calculateTrendPercentage(List<NetWorthHistoryPoint> history)` - calculates change from first to last data point
Add utility method `_parseDouble(dynamic value)` following pattern from assets_tabs_page.dart. This service centralizes all dashboard data transformations.</action>
  <verify>flutter analyze passes with no errors</verify>
  <done>Aggregation service ready to transform raw API data into chart-ready format</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `flutter analyze` passes with no errors
- [ ] `flutter build apk --debug` succeeds
- [ ] New API method follows existing IgnApiService patterns
- [ ] Domain models are immutable with fromJson factories
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Historical API endpoint available for dashboard charts
- Data models ready for visualization layer
- Aggregation service centralizes data transformations
</success_criteria>

<output>
After completion, create `.planning/phases/04-assets-liabilities-dashboard/04-01-SUMMARY.md`:

# Phase 04 Plan 01: Assets Data Layer and Aggregation Summary

**[Substantive one-liner - what shipped, not "phase complete"]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `path/to/file.dart` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 04-02-PLAN.md
</output>
